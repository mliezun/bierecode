---
import Layout from '../../layouts/main-layout.astro';
import { UpdateDetail } from '../../components/pages/updates/components/update-detail/update-detail';

const id = Astro.url.searchParams.get('id');
---

<Layout>
  <UpdateDetail client:load updateId={id ?? ''} />
</Layout>
